# Placeholder-only fixture for Azure Disk CSI scenarios.
# Replace <REPLACE_WITH_*> values before applying to your own AKS cluster.
# No real resource group names, storage account keys, or Azure tenant IDs are stored here.
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: managed-premium
provisioner: disk.csi.azure.com
volumeBindingMode: WaitForFirstConsumer
parameters:
  storageaccounttype: Premium_LRS
  kind: Managed
allowVolumeExpansion: true
