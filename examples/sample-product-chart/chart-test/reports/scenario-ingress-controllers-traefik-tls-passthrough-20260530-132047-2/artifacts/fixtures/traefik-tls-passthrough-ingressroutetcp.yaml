---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: sample-tls-passthrough
  namespace: sample
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`sample.test.local`)
      services:
        - name: sample
          port: 443
  tls:
    passthrough: true
