---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-app-label
spec:
  validationFailureAction: Enforce
  background: true
  rules:
    - name: require-app-kubernetes-io-name
      match:
        any:
          - resources:
              kinds:
                - Deployment
      validate:
        message: "Deployments must have the 'app.kubernetes.io/name' label"
        pattern:
          metadata:
            labels:
              app.kubernetes.io/name: "?*"
