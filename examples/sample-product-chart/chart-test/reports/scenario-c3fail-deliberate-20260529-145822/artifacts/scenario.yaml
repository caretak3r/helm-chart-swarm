---
id: c3fail-deliberate
name: "Deliberately failing scenario"
description: "Validates partial artifacts on failure — uses a bad assert"
labels: {test: deliberate-fail}
cluster:
  provider: kind
  k8s_version: v1.30.0
product:
  chart: ./chart
  release: sample
  namespace: sample
asserts:
  - {type: helm-status-deployed, release: nonexistent-release, namespace: nonexistent-ns}
tags: [test]
mechanisms: ['addon:none']
